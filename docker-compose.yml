version: "3.8"
services:
  loto6:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./output:/app/output
      - ./.env:/app/.env
    environment:
      - TZ=Asia/Tokyo
